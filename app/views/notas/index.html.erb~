<p id="notice"><%= notice %></p>

<h1>Listing Notas</h1>

<table>
  <thead>
    <tr>
      <th>Aluno</th>
      <th>Disciplina</th>
      <th>P1</th>
      <th>P2</th>
      <th>P3</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @notas.each do |nota| %>
      <tr>
        <td><%= nota.aluno.nome %></td>
        <td><%= nota.disciplina.nome %></td>
        <td><%= nota.p1 %></td>
        <td><%= nota.p2 %></td>
        <td><%= nota.p3 %></td>
        <td><%= link_to 'Show', nota %></td>
        <td><%= link_to 'Edit', edit_nota_path(nota) %></td>
        <td><%= link_to 'Excluir', nota, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Nota', new_nota_path %>
